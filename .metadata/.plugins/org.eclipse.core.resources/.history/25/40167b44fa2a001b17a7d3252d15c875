package Model;

import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.util.ArrayList;
import java.util.List;



import Helper.DatabaseUtil;



public class DAO {
public List<DTO> getListBook(String sql){
	List<DTO> listBook = new ArrayList<>();
	PreparedStatement ps = DatabaseUtil.getInstance().conn.prepareStatement(sql);
	ResultSet rs = ps.executeQuery();
	while (rs.next()) {
		DTO book = new DTO();
		book.setIdBook(rs.getInt(1));
		book.setNameBook(rs.getString(2));
		book.setAuthorBook(rs.getString(3));
		book.setCategoryBook(rs.getString(4));
		listBook.add(book);
	}

	return listBook;
}
	
	
}

